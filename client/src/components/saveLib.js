import React from "react";

function SaveLib() {
    return(
        <div>
            <p>list of saved libs here with a delete button on each list item</p>
        </div>
    );
}

export default SaveLib;